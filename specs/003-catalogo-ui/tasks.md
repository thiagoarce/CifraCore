# Tasks 003 — Catálogo e UI Base

Depende de: 001 (auth/schema), 002 (dados importados para testar com conteúdo real).

- [x] **T1 `[SONNET]` Design system** — config Tailwind com tokens semânticos das duas paletas (spec §Design System), store `$theme` (dark padrão, persistência, classe no `<html>`), componentes `ui/` básicos (Button, Card, Tabs, Modal, Input, Badge).
      **DoD:** cenário "Tema padrão" passa; nenhum hex fora da config; página de amostra exibe todos os componentes nos dois temas.
      _Feito. Tokens em `src/routes/layout.css` via `@theme` (Tailwind v4; decisão registrada no plan — dark é o default em `:root`, `.light` sobrescreve, sem uso de `dark:` em componente nenhum). Store `src/lib/stores/theme.ts` com 5 testes (`theme.spec.ts`, mesmo padrão do `currentBand.spec.ts`). Componentes em `$lib/components/ui/`: `Button`, `Card`, `Badge`, `Input`, `Tabs`, `Modal` (usa `<dialog>` nativo), `ThemeToggle`. Página de amostra em `/dev/ui-kit` (não linkada em nenhuma navegação, só acessível por URL — decisão registrada no plan). Verificado com Playwright contra o stack local nos dois temas (screenshots) e interações (toggle de tema, abrir/fechar modal por botão/ESC/backdrop). Achado e corrigido durante o QA: `<dialog>` não centralizava (Preflight zera a `margin: auto` default do UA) — `m-auto` explícito resolveu; backdrop trocado de um tom do tema pra `black/50` puro (o tom escuro do tema ficava invisível sobre o próprio fundo escuro)._
      _Bug não relacionado ao design system, mas encontrado testando as telas de auth neste QA: login/cadastro travavam após autenticar com sucesso (race condition real entre `invalidate()`+`goto()` do SvelteKit, não só o loading infinito já corrigido antes). Detalhe completo e correção (migração pra form actions) registrados em `specs/001-fundacao/tasks.md` T5, já que é bug da feature 001, não da 003 — só apareceu aqui porque foi neste QA visual que as telas de auth foram exercitadas de ponta a ponta pela primeira vez com Playwright._

- [x] **T2 `[SONNET]` AppShell + Dashboard** — layout `(app)` com Sidebar (seletor de banda usando `$currentBand`, navegação), dashboard com lista/busca de músicas e atalho "Importar".
      **DoD:** dashboard lista só músicas da banda ativa; busca filtra por título/artista; navegação mobile funciona (sidebar colapsável).
      _Feito. `$lib/components/layout/Sidebar.svelte` (seletor de banda via `<select>`, nav Catálogo/Bandas/Importar, e-mail + logout + `ThemeToggle`) + `(app)/+layout.svelte` vira o AppShell de verdade (drawer off-canvas no mobile com backdrop, coluna fixa no desktop `md:`). Dashboard reescrito: busca de músicas da banda ativa é **client-side** (`$effect` reagindo a `$currentBand`, mesmo padrão já usado no dedupe da 002 — não dá pra ser `+page.server.ts` porque a banda ativa só existe no `localStorage`, o servidor nunca sabe qual é; decisão registrada no plan), busca por texto filtra em `$derived` local (catálogos pequenos, conforme plan). Atalho "Importar música" resolve o link que faltava (mencionado antes desta fase)._
      _Corrigida a mesma race condition do login (T1) em mais três fluxos que tinham o padrão `invalidate()`+`goto()`: **logout** (`dashboard/+page.svelte`), **criar banda** (`bands/+page.svelte`) e **sair da banda** (`bands/[id]/+page.svelte`) — todos travavam silenciosamente antes da correção (confirmado um a um com Playwright). Todos migrados para form actions (`(app)/logout/+page.server.ts` novo — só existe pra ser o alvo POST do form de logout da Sidebar; `bands/+page.server.ts`; `bands/[id]/+page.server.ts`, ação nomeada `leave` já que a página também tem o form de convidar membro, que não navega e ficou como estava). `currentBand.clear()` no `use:enhance` do logout, já que so recarregar a página NÃO limpa o localStorage — sem isso, a banda ativa vazaria para a próxima conta logada no mesmo navegador._

- [x] **T3 `[SONNET]` Tela da música** — rota `songs/[id]` conforme wireframe: `SongHeader` (controles de tom desabilitados), `InstrumentTabs` (aba padrão pela preferência de instrumento), `AstRenderer` texto puro (fonte mono, `whitespace-pre`, `{#each}` com keys, badge de repetição "Nx"), `VoiceSelector` com opacidade de voz inativa, `FloatingFooter` com placeholders.
      **DoD:** cenários "Aba do instrumento preferido" e "Voz ativa em dueto" passam; usável em viewport de celular.
      _Feito. `songs/[id]/+page.ts` (universal load, mesmo padrão do `bands/[id]`) busca `song` + `song_tabs` por id da URL e cruza com `data.bands` (do layout) pra achar `isAdmin`/`memberInstrument` dessa banda — RLS garante o escopo, a UI só exibe. Novo store `$lib/stores/preferredInstrument.ts` (localStorage, com testes) implementa a prioridade do R4: preferência salva → instrumento do músico na banda → primeira tab disponível; qualquer tab escolhida (clique ou default computado) vira a nova preferência. `AstRenderer` usa `<pre>` de verdade (preserva espaço/alinhamento nativamente, sem precisar de `whitespace-pre` redundante) com `{#each blocks as block (block.id)}`; nunca lança mesmo com `content` malformado (cast defensivo, Lei 4). Tab de instrumento com `content_type: 'pdf_url'` renderiza desabilitada com sufixo "(PDF)" (texto, não emoji de cadeado — regra `no-emoji-icons` da skill) até a fase 008. Dois cenários Gherkin verificados com Playwright contra dados reais no stack local: preferência de instrumento (`localStorage` = bass → aba "Baixo" ativa) e voz de dueto (selecionar "João" deixa os blocos de "Maria" com opacidade reduzida). Viewport de celular (375px) conferido com screenshot — usável, sem scroll horizontal na música de teste._

- [ ] **T4 `[SONNET]` Edição de música** — admin edita metadados (title, artist, keys, capo, bpm) e reabre blocos no componente de Rascunho da 002 (reuso, não cópia).
      **DoD:** edição persiste; member não vê botões de edição (e RLS bloqueia por baixo).
