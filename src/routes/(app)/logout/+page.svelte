<script lang="ts">
	// Never rendered: load() above always redirects on GET, and the action
	// always redirects on POST. Exists only so SvelteKit treats this as a page
	// route (form actions require a +page.svelte alongside +page.server.ts).
</script>
